import { DescriptionArticlePipe } from './description-article.pipe';

describe('DescriptionArticlePipe', () => {
  it('create an instance', () => {
    const pipe = new DescriptionArticlePipe();
    expect(pipe).toBeTruthy();
  });
});
