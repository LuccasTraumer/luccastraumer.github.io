import { DefinitionDatePipe } from './definition-date.pipe';

describe('DefinitionDatePipe', () => {
  it('create an instance', () => {
    const pipe = new DefinitionDatePipe();
    expect(pipe).toBeTruthy();
  });
});
